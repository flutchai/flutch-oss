{
  "type": "object",
  "properties": {
    "systemPrompt": {
      "type": "string",
      "title": "System Prompt",
      "format": "textarea",
      "default": "You are a consultative sales assistant. Help the customer understand their problem and guide them toward a solution through natural conversation."
    },
    "modelId": {
      "type": "modelSelector",
      "title": "Model",
      "params": {
        "modelType": "chat",
        "isActive": true
      }
    },
    "temperature": {
      "type": "number",
      "title": "Temperature",
      "default": 0.7,
      "minimum": 0,
      "maximum": 1
    },
    "maxTokens": {
      "type": "integer",
      "title": "Max Tokens",
      "default": 2048,
      "minimum": 100,
      "maximum": 4000,
      "description": "Maximum tokens in response"
    },
    "availableTools": {
      "type": "toolSelector",
      "title": "Available Tools",
      "description": "Select which tools the agent can use during conversations",
      "params": {
        "isMulti": true,
        "showDescriptions": true
      }
    },
    "crm": {
      "type": "object",
      "title": "CRM Integration",
      "description": "Configure contact synchronization with CRM",
      "properties": {
        "lookupBy": {
          "type": "string",
          "title": "Contact Lookup Field",
          "description": "Which field to use for finding existing contacts",
          "enum": ["email", "phone"],
          "default": "email"
        },
        "writeFields": {
          "type": "array",
          "title": "Write Fields",
          "description": "Fields to write back to CRM after conversation. Leave empty to write all non-system fields.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["lookupBy"]
    }
  },
  "required": ["systemPrompt", "modelId"]
}
