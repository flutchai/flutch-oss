nodeLinker: node-modules

# Suppress warnings for optional peer deps we intentionally don't install
packageExtensions:
  "ts-loader@*":
    peerDependenciesMeta:
      webpack:
        optional: true
  "langfuse-langchain@*":
    peerDependenciesMeta:
      langchain:
        optional: true
